# 示例硬编码文本映射文件 - 用于测试
# 请审核以下内容，修改英文翻译，并将 approved 设置为 true
# 只有 approved: true 的条目会被处理

ui_text_mappings:
  ui_text_widget:
    # 示例：简单的文本替换
    works_text_示例文本:
      text_zh: "示例文本"
      text_en: "Sample Text"
      file: "test_file.dart"
      line: 10
      context_type: "ui_text_widget"
      detection_type: "ui_text"
      approved: true  # 已审核通过

  ui_properties:
    # 示例：UI属性文本
    common_hint_请输入内容:
      text_zh: "请输入内容"
      text_en: "Please enter content"
      file: "test_file.dart"
      line: 15
      context_type: "ui_properties"
      detection_type: "ui_text"
      approved: true  # 已审核通过

enum_mappings:
  enum_based:
    # 示例：枚举显示名称
    WorkStatus:
      enum_workstatus_completed:
        text_zh: "已完成"
        text_en: "Completed"
        enum_name: "WorkStatus"
        enum_value: "completed"
        file: "models/work_status.dart"
        line: 8
        detection_type: "enum_based"
        approved: true  # 已审核通过

      enum_workstatus_pending:
        text_zh: "待处理"
        text_en: "Pending"
        enum_name: "WorkStatus"
        enum_value: "pending"
        file: "models/work_status.dart"
        line: 12
        detection_type: "enum_based"
        approved: false  # 尚未审核，不会被处理
